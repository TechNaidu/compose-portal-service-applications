#!/usr/bin/env bash
# Repository-level git hook (alternative to installing into .git/hooks)
set -euo pipefail
ROOT_DIR=$(git rev-parse --show-toplevel 2>/dev/null || echo "${PWD}")
$ROOT_DIR/scripts/detect-aws-creds.sh

